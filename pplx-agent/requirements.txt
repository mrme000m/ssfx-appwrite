# PPLX Agent runtime dependencies.
# These are kept intentionally close to the remote-services package so the
# project can be run from a single virtual environment.

# Perplexity client dependencies
curl-cffi>=0.15.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# API server
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.27.0

# TradingView free-tier scanner
requests>=2.31.0

# Appwrite config source-of-truth
appwrite>=21.0

# Async utilities
aiohttp>=3.9.0

# Date/time and utility
python-dateutil>=2.9.0
